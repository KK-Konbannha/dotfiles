def install_apps(DIST):
    # pip install pillow
    # pip install black flake8 pyright
    # nvm
    # NVM_URL = "https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.1/install.sh"
    # p1= subprocess.Popen(["curl", "-o-", NVM_URL], stdout=subprocess.PIPE)
    # p2 = subprocess.Popen(["bash"], stdin=p1.stdout, stdout=subprocess.PIPE)
    # p1.stdout.close() # type: ignore
    # print(p2.communicate()[0].decode().strip().split('\n'))
    #
    # os.environ["NVM_DIR"] = f"{HOME}/.nvm"
    # subprocess.run(["$NVM_DIR/nvm.sh"])  # This loads nvm

    # npm
    # nvm install node
    # echo "$(tput setaf 2)Installing node & npm complete!. ✔︎$(tput sgr0)"

    # yarn
    # node_path=`nvm which current | sed "s/\/node\$//"`
    # npm install -g yarn
    # echo $(tput setaf 2)Installing yarn complete!. ✔︎$(tput sgr0)

    # ghcup
    # curl --proto '=https' --tlsv1.2 -sSf https://get-ghcup.haskell.org | sh
    # echo $(tput setaf 2)Installing ghcup complete!. ✔︎$(tput sgr0)
    pass
